# 隐私执行规则

- 输入只在本次任务的临时目录流转；默认不写入长期记忆、日志、云端或知识库。
- 文件名使用 `subject_code`，不使用真实姓名、身份证号、手机号或完整地址。
- 交付前删除 `input.json`、`chart.json`、`chart.txt` 和分析中间文件；PDF 是否保留由用户决定。
- 报告需要展示生辰时，先确认用户同意；默认地点降到城市级。
- 任何错误输出中不得打印完整生辰。可使用 `[REDACTED_BIRTH_DATA]` 代替。
