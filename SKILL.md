---
name: bazi-report-pdf-rheadesign
description: 通过确定性八字与紫微斗数排盘，生成隐私优先、简洁高级且可渲染核验的多语言 PDF 人生主题报告；适用于用户提供代号、生辰、性别和地点并要求趋势、宜忌或生活建议时。
---

# 八字 + 紫微 PDF 报告

这个技能把传统八字、紫微斗数作为文化解释框架，输出一份结构化、多语言 PDF。它不是医学、心理、投资、法律或婚姻决策工具，也不能宣称科学预测能力。报告要回答一个成年人真正关心的问题：我是什么样的人、惯用的应对方式是什么、人生各模块可能怎样展开、眼下和接下来几年该如何安排。传统术语必须在首次出现时翻译成人话。

## 输入与隐私

先收集并确认以下字段：

- `subject_code`：代号优先，不要求真实姓名。
- `birth.calendar`：`solar`（公历）或 `lunar`（农历）；农历要确认是否闰月。
- `birth.date`、`birth.time`：日期 `YYYY-MM-DD`，时间 `HH:MM`（24 小时制）；缺时辰时必须追问，不得默认子时。
- `birth.gender`：男/女（用于大运顺逆与部分紫微规则）；不愿提供时，说明哪些模块无法计算，不得猜测。
- `birth.place`、`current_location`：城市/国家即可。默认只作为报告上下文与方位建议依据，不默默进行经度真太阳时校正。
- `as_of_date`：本次报告的“截至日期”，格式 `YYYY-MM-DD`。每次运行都重新核对；未提供时读取系统当前日期并让用户确认。
- `language` / `locale`：报告语言和地区（如 `zh-CN`、`en-US`、`ja-JP`、`fr-FR`）。默认跟随用户语言；未指定时使用中文。八字、紫微、十神、四化、大运等术语保留中文，并在首次出现时用目标语言解释。

出生地、现居地和精确出生时间属于敏感信息。除生成本次报告所需的临时文件外，不写入记忆、日志、知识库或第三方服务；文件名使用代号，不用真实姓名。生成 PDF 前提示用户：PDF 本身会包含其选择展示的生辰字段，需由用户自行保管。默认展示城市级地点，除非用户明确要求更精确。

## 工作流

需要让他人填写时，先打开 `templates/input-form.html`。这是离线表单，提交后下载标准 `input.json`；然后只需把文件上传，并发送一句固定要求即可。完整的面向用户操作手册见 [user-guide.md](references/user-guide.md)。

1. **信息确认与范围**：确认公历/农历、闰月、时区、性别和本次 `as_of_date`；若用户没有另行指定，分析八字 + 紫微综合版。报告必须覆盖人格底色、优势与盲点、健康与压力、事业/工作方式、适合行业、财富与资源、感情与亲密关系、父母与家庭、社交与合作、子女/创造力（适用时）、五行与方位、完整十年大运、未来 3 年、未来 3 个月、日常提运技巧和长期注意事项。每个模块要回答“盘面看到什么 → 现实中可能怎样表现 → 对成年人有什么用 → 可以怎样做”，写到读者能想象具体场景，而不是只列形容词。
2. **确定性排盘**：绝不凭模型记忆手排。执行：

   ```bash
   node scripts/run_chart.js --input=input.json --output=chart.json
   node calculator/dist/dump-text.js --input=chart.json --output=chart.txt
   ```

   `scripts/run_chart.js` 会把农历（含闰月）转换为公历后再调用随技能提供的算法层。算法层的 stdout 只保存 JSON，stderr 仅用于错误诊断。若依赖缺失，进入 `calculator/` 执行 `npm install` 后重试；不要改用手算。
3. **交叉解读**：以 `chart.txt` 为唯一盘面事实源，分别核对八字的日主、格局、旺衰、调候、十神、刑冲合害、大运，与紫微的命宫、身宫、十二宫、生年四化和大限。只在两盘均支持或明确说明冲突时下结论；流派差异、地点/时刻不确定性集中写进顶层 `overall_note`，不要在每一章重复“置信度”。格局必须按月令、透干、根气、制化与组合关系逐一判断，允许结论为“成立”“有条件成立”“兼格/局部关系”或“不取此格”；不能因为某个十神出现就直接套用格局名称。检查范围应覆盖命局实际出现的正格、变格、专旺/从格及常见组合（如伤官配印、官印相生、杀印相生、食神制杀、财官印食、从财、从杀等），但只输出与当前盘面证据相符的格局。每个格局都要写清：证据链、成格条件是否齐备、日主身强身弱如何放大或缓冲其作用，以及进入每一轮大运后可能出现的现实主题。
4. **生成内容**：先执行 `python3 scripts/date_windows.py --as-of=YYYY-MM-DD`，把输出的日期窗口原样写入 `report.json` 的 `forecast`。未来 3 年从截至日期所在年份起算，未来 3 个月从截至日期到三个月后的同日/月底；每次输入重新计算。按 [report-outline.md](references/report-outline.md) 生成结构化 `report.json`。每个模块按固定阅读顺序写：第一段放有出处的古籍原文节录（保留原文和书名，不把现代总结冒充古文）；第二段起用现代汉语解释，落到成年人会遇到的具体场景；最后给出可执行建议。八字和紫微术语第一次出现时顺手解释，例如“命宫（紫微里观察人生主轴和自我表现的宫位）”“化禄（把资源、机会或顺手程度推高的象征）”。每章不再重复写置信度或免责声明；在前面的“整体阅读说明”集中说明模型边界，章节正文专注于解释和场景。每个模块应有 2–5 个有信息量的段落、清楚的小标题和 2–4 条具体建议，少用“不是……而是……”等模板句，避免“人话解读”“赋能”“底层逻辑”等机器化说法。目标为 8–10 页 A4；若完整内容超过 10 页，允许扩展到 20 页，不得为压页删掉必要模块。健康部分只写体质/作息/压力倾向，并建议必要时咨询合格专业人士。
在生成 `sections` 时，格局专题至少单列一节，写出“结构链 → 身强身弱 → 大运触发 → 生活现象”的完整推演；完整十年大运章节逐运复用这条链，不只列干支和吉凶。

**标题规则**：每个模块标题都写成总结型陈述句，直接概括该节的盘面判断与现实主题，例如“伤官格为主，专业判断要通过制度与边界转化为收入”“收入可以增长，但必须分清投入、资产与责任”。标题不使用问句、冒号、栏目式提问或“你是什么样的人/适合什么”等泛化句式。

**命主专属与去重**：不同命主必须重新依据各自的 `chart.txt` 写作。不得复制另一份报告的标题、段落、建议或中后段模板；健康、财务、未来窗口、提运和长期注意事项都要落到当前命局的日主、格局、刑冲、紫微宫位和大运。若手边有其他临时 `report.json`，生成后运行 `python3 scripts/check_uniqueness.py --input=report.json --against=other-report.json`；检查通过后再删除中间文件。古籍短引、固定免责声明和字段标签可以共用，其余实质性文字应保持命主专属。

5. **渲染 PDF**：

   ```bash
   python3 scripts/render_pdf.py --input=report.json --output=output.pdf --max-pages=20
   ```

   输入字段和可选表格见 [report-schema.md](references/report-schema.md)。脚本会嵌入可用的中文字体、生成封面/排盘摘要/正文/免责声明，并以代号命名输出。正文目标 8–10 页；`max-pages=20` 只是安全上限，不是压缩目标。

   视觉遵循 [visual-style.md](references/visual-style.md) 的三色近似色规范。
6. **可交付核验**：运行 `pdfinfo output.pdf`、`pdftotext output.pdf -`，并用 `pdftoppm -png output.pdf tmp/pdf-check/page` 渲染页面检查中文、表格、分页、页眉页脚和是否有截断/黑方块。检查通过后再把 PDF 路径交给用户，并删除包含原始生辰的中间 JSON/TXT；若用户明确要保留，说明其隐私风险。

## 内容边界

- “权威”只能表述为所采用传统流派/算法的来源与版本，不能表述为医学或科学验证的预测。
- 未来时间必须相对于本次已核对的 `as_of_date` 实时计算；不得沿用上一次运行的日期，也不得把某个年份写死。报告中明确显示截至日期、3 年窗口和 3 个月窗口。
- “宜/忌”写成低风险的行动提示（节奏、沟通、复盘、作息、预算），不下投资、辞职、结婚、治疗等命令。
- **去 AI 化复核**：删掉空泛开场、宣传腔、机械三段式、连续“此外/同时/因此”、过度粗体、表情符号和破折号；句子长短交替，优先具体动词和生活场景，直接说结论。不要为了“全面”重复同一判断，也不要用金句或宿命式收尾。
- **版面与篇幅**：正文目标 8–10 页 A4，完整性优先；如果个案需要更多解释，可延长至 20 页。若超过 20 页，先合并重复句、删掉空泛修辞，再考虑分页；不得缩小到难以阅读，也不得删掉健康、事业、感情、家庭、财富、时间走势或提运模块。
- **国际化排版**：优先使用覆盖目标语言的 Unicode 字体；阿拉伯文、希伯来文和部分印度文字需要对应字体与 RTL/字形整形依赖。缺少依赖时明确提示用户安装，不输出方框或乱码。
- 出现自伤、伤害他人、下蛊、断人财路等请求时拒绝相关推演并转向现实支持。
- 报告末尾固定加入：

  > 本报告基于传统八字与紫微斗数理论框架，仅供文化研究与娱乐参考，不构成医疗、投资、婚姻、法律或其他人生决策依据。命运由个人选择与客观环境共同塑造。
